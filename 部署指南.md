# 本地项目部署到 GitHub Pages 完整指南

## 一、前提准备

1. 注册 GitHub 账号：[github.com](https://github.com)
2. 电脑装好 Git：[git-scm.com](https://git-scm.com) 下载安装
3. 项目文件夹准备好（比如你的 `D:\Blog`）

## 二、创建 GitHub 仓库

1. 打开 [github.com/new](https://github.com/new)
2. **仓库名**：必须填 `你的用户名.github.io`
   - 比如你的用户名是 `star-YP`，就填 `star-yp.github.io`
3. 其他选项全部留空，**不要勾选任何初始化文件**
4. 点 **Create repository**

## 三、把项目推上去

打开命令行（PowerShell 或 Git Bash），进入项目文件夹：

```bash
# 1. 初始化 Git
cd D:/Blog          # 进入你的项目目录
git init            # 让这个文件夹变成 git 仓库
git checkout -b main  # 切到 main 分支

# 2. 创建 .gitignore（排除不需要上传的文件夹）
echo ".claude/\nnode_modules/\nworker/" > .gitignore

# 3. 提交所有文件
git add -A
git commit -m "第一次提交"

# 4. 关联远程仓库（换成你自己的仓库地址）
git remote add origin https://github.com/你的用户名/你的用户名.github.io.git

# 5. 推送
git push -u origin main
```

> **如果 push 失败**（国内网络问题），需要配代理：
> ```bash
> git config http.proxy http://127.0.0.1:你的代理端口
> git push -u origin main
> ```

## 四、开启 GitHub Pages

1. 打开你的仓库 → **Settings** → **Pages**
2. **Branch** 选 `main`，点 **Save**
3. 等一两分钟，你的网站上线：
   > `https://你的用户名.github.io`

## 五、以后更新代码

每次改完代码，三行命令：

```bash
git add -A
git commit -m "描述改了什么"
git push
```

推送后 GitHub Pages 自动重新部署，一两分钟生效。

## 六、域名（可选）

如果想用自己的域名，在 GitHub Pages 设置里填 `Custom domain`，然后去域名服务商那里加一条 CNAME 记录指向 `你的用户名.github.io`。

---

## 常见问题

| 问题 | 解决 |
|------|------|
| push 超时 | 配 git 代理：`git config http.proxy http://127.0.0.1:7897` |
| 页面 404 | 检查仓库名是否是 `用户名.github.io` |
| 改了代码不生效 | 等 1-2 分钟，GitHub Pages 有延迟；或硬刷新（Ctrl+F5） |
| 图片不显示 | 检查路径是相对路径（`assets/photo.jpg`），不是绝对路径 |
